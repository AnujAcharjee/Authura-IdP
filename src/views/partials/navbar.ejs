<nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200/60 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">

      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 text-xl font-bold text-slate-900 hover:text-orange-600 transition-colors">
        <%- include("../icons/logo") %>
        <span class="hidden sm:inline">Pramaan</span>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center gap-6">
        <a href="/account" class="text-sm font-medium text-slate-600 hover:text-orange-600 transition-colors">
          Dashboard
        </a>
        <a href="/docs" class="text-sm font-medium text-slate-600 hover:text-orange-600 transition-colors">
          Documentation
        </a>
      </div>

      <!-- Desktop Auth -->
      <div class="hidden md:flex items-center gap-4">
        <% if (typeof user !== 'undefined' && user) { %>
        <%- include("./user-menu", { user }) %>
        <% } else { %>
        <a href="/signin" class="text-sm font-medium text-slate-600 hover:text-orange-600 transition-colors">
          Sign in
        </a>
        <a href="/signup" class="inline-flex items-center justify-center px-4 py-2 rounded-lg bg-linear-to-r from-orange-500 to-orange-600 text-white text-sm font-semibold hover:from-orange-600 hover:to-orange-700 transition-all shadow-sm hover:shadow-md">
          Get Started
        </a>
        <% } %>
      </div>

      <!-- Mobile Menu Button -->
      <button id="mobileMenuBtn" class="md:hidden inline-flex items-center justify-center p-2 rounded-lg text-slate-700 hover:bg-slate-100 focus:outline-none">
        <svg id="menuIcon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>

    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobileMenu" class="md:hidden hidden border-t border-slate-200 bg-white/95 backdrop-blur-md">

    <div class="px-4 py-4 space-y-4">

      <a href="/account" class="block text-sm font-medium text-slate-700 hover:text-orange-600 transition-colors">
        Dashboard
      </a>

      <a href="/docs" class="block text-sm font-medium text-slate-700 hover:text-orange-600 transition-colors">
        Documentation
      </a>

      <div class="pt-4 border-t border-slate-200">
        <% if (typeof user !== 'undefined' && user) { %>
        <%- include("./user-menu", { user }) %>
        <% } else { %>
        <a href="/signin" class="block text-sm font-medium text-slate-700 hover:text-orange-600 transition-colors mb-3">
          Sign in
        </a>

        <a href="/signup" class="block text-center px-4 py-2 rounded-lg bg-linear-to-r from-orange-500 to-orange-600 text-white text-sm font-semibold hover:from-orange-600 hover:to-orange-700 transition-all shadow-sm">
          Get Started
        </a>
        <% } %>
      </div>

    </div>
  </div>
</nav>