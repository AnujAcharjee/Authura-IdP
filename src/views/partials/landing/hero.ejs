<section>
  <div class="max-w-6xl mx-auto px-6 sm:px-8 lg:px-12 py-15 sm:py-20">
    <div class="max-w-3xl mx-auto flex flex-col items-center text-center">

      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-orange-50 border border-brand-orange/70 text-sm font-mono font-semibold text-brand-orangeDark mb-8">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-brand-orange opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-brand-orangeDark"></span>
        </span>
        OAuth 2.0 & OpenID Connect
      </div>

      <!-- Heading -->
      <div class="relative inline-block">
        <div class="absolute inset-0 blur-3xl bg-orange-400 opacity-40"></div>

        <h1 class="relative font-brand-hero text-4xl sm:text-6xl font-semibold tracking-tight text-slate-900">
          Modern Identity Infrastructure
        </h1>
      </div>

      <!-- Subheading -->
      <p class="mt-6 text-md sm:text-lg text-slate-600 leading-relaxed max-w-xl lg:max-w-3xl text-center">
        Secure authentication, token lifecycle management, and standards-compliant
        OAuth 2.0 + OpenID Connect flows for web, mobile, and backend systems.
      </p>

      <!-- CTA -->
      <div class="mt-10 flex flex-col sm:flex-row gap-4">
        <a href="/account" class="btn-orange">
          Get Started
        </a>
      </div>

      <!-- Feature Row -->
      <div class="mt-14 flex flex-col items-start gap-4 font-sans font-semibold
            lg:flex-row lg:items-center lg:flex-wrap lg:justify-center lg:gap-x-8 lg:gap-y-4 
            text-sm text-slate-500">
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500 border-green-400"></span>
          PKCE & Refresh Token Rotation
        </div>
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500 border-green-400"></span>
          OIDC Discovery & JWKS
        </div>
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-emerald-500 border-green-400"></span>
          Client & Scope Management
        </div>
      </div>

    </div>
  </div>
</section>